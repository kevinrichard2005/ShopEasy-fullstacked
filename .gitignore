# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# Database - ignore DB file but keep folder via .gitkeep
instance/*.db
# Keep the .gitkeep so Render/Heroku sees the folder
!instance/.gitkeep

# Uploaded images (user uploaded) — but keep seeded defaults
static/images/*
!static/images/.gitkeep

# OS
.DS_Store
Thumbs.db

# Flask
.flask_env

# IDEs
.vscode/
.idea/
